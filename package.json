{
  "name": "nearbaz",
  "version": "0.1.0",
  "private": true,
  "description": "NearBaz — Your Local Business Network (hyperlocal multi-shop marketplace)",
  "workspaces": [
    "packages/*",
    "api",
    "apps/*"
  ],
  "scripts": {
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f db",
    "shared:build": "npm run build --workspace @nearbaz/shared",
    "api:dev": "npm run start:dev --workspace @nearbaz/api",
    "api:test": "npm run test --workspace @nearbaz/api",
    "render:build": "npm install && npx prisma generate --schema api/prisma/schema.prisma && npm run build --workspace @nearbaz/shared && npm run build --workspace @nearbaz/api-client && npm run build --workspace @nearbaz/api",
    "render:start": "node api/dist/main.js",
    "eas-build-post-install": "npm run build --workspace @nearbaz/shared && npm run build --workspace @nearbaz/api-client"
  },
  "engines": {
    "node": ">=20"
  }
}
