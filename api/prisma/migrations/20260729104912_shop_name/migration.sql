-- Add the Shop.name display column (shown in discovery + storefront).
--
-- NOTE: Prisma's autogenerated diff also tried to DROP the PostGIS `geog`
-- columns + GIST indexes (it doesn't track them, since they're added by a raw
-- migration and aren't in schema.prisma). Those drops were REMOVED by hand —
-- geog is a deliberate out-of-band PostGIS column (see schema.prisma NOTE and
-- the 20260729103539_postgis_geog_columns migration). This migration only adds
-- the name column.
--
-- Safe as NOT NULL with no default because the Shop table is empty at this
-- point (expand-then-contract, schema rule #7).
ALTER TABLE "Shop" ADD COLUMN "name" TEXT NOT NULL;
